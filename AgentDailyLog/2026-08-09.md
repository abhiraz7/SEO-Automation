# AgentLog — 2026-08-09

## Session: Gemini + dual-provider Backlinks + Competitor Analysis + WordPress plugin review

---

### What We Did (build work — see AgentLog.md for full technical detail)
- Added Gemini as a second, switchable AI provider (`app/ai_provider.py`, `app/gemini.py`)
- Wired DataForSEO into Backlinks as a second data source, sharing the same global Data Provider switch as on-page audits
- Found and fixed a real bug: the Backlinks panel lived on a page unreachable for any real project in this account
- Built Competitor Analysis module, Phase 1-3 (foundation): competitor management + basic overview comparison table
- Full architecture audit against an external brief
- Investigated a real client-reported bug (deployed title fix "not appearing") through to root cause: the WordPress plugin only supports Yoast SEO, but the actual client site (`examnotespdf.in`) runs RankMath — the write succeeded, but to a meta key nothing reads
- Reviewed the WordPress companion plugin's source (`claude-wp-mcp final/`) for security: auth is solid (timing-safe token comparison, rate limiting, real server-side action-group gating); found `php_exec`'s "confirmation" token provides no real protection against a leaked API token (only prevents accidental calls)
- Corrected an earlier claim: the plugin's existing `flush_cache` tool only clears WordPress's *object* cache, not page-caching plugins (LiteSpeed, WP Rocket, etc.) — would not have fixed the RankMath investigation's caching red herring
- Scoped (not yet built) a new, narrower "VtechSEO-Plugin" — SEO + security + monitoring only, no page-builder control, no `php_exec` — safer to install fleet-wide across every client site than the existing general-purpose dev plugin

### Files changed today
See `AgentLog.md`'s 2026-08-09 entry for the full file list (31 files, first commit `407b5ba`) — plugin review work today was read-only investigation, no plugin code was modified.

---

## Decision Questions for the Client

Compiled from everything surfaced today that needs a business/product decision, not a technical one. Each has a proposed default so the client can just confirm or override.

| # | Question | Why it matters | Proposed solution |
|---|---|---|---|
| 1 | Top up Semrush's API balance (currently 0 units), or commit fully to DataForSEO? | Semrush is silently blocking Organic Keywords data, Semrush-path backlinks, and will block anything built on top of it going forward. Confirmed via live 403s today, not assumed. | **Commit to DataForSEO as primary**, keep Semrush as an optional secondary if/when funded again — DataForSEO already covers Backlinks, on-page, and is funded ($49 balance confirmed live today). |
| 2 | Should the new lean "VtechSEO-Plugin" (SEO + security + monitoring, no page-builder/PHP-exec) be the one installed on every client site by default? | Smaller attack surface for a fleet-wide install vs. the existing general-purpose `claude-wp-mcp` dev tool, which includes a real RCE-capable tool (`php_exec`). | **Yes** — ship the lean plugin to all clients; keep `claude-wp-mcp` as an internal, engagement-specific dev tool only, never installed by default. |
| 3 | Should `php_exec` ever be enabled on a real client production site? | It's excluded by default and its "confirmation" step doesn't add real protection against a leaked token — full PHP execution if that token ever leaks. | **No, staging/dev sites only**, and only for the duration of active development work — disable again afterward. |
| 4 | Who provisions and pays for the new external API keys needed (Google PageSpeed Insights, Chrome UX Report, Google Safe Browsing, WPScan)? | All are real, verified-working APIs, but need a Google Cloud project + WPScan account, each with its own free-tier limits. | **Vtechys provisions one shared set of keys** (same pattern as the existing Anthropic/Gemini/DataForSEO keys), tracked as a standard cost-of-goods line, not per-client. |
| 5 | Should SEO-plugin support (Yoast/RankMath/AIOSEO) be built as one generic "read/write any post meta key" tool, or a separate purpose-built handler per SEO plugin? | Generic = less plugin code to maintain long-term, all the "which plugin, which field" logic stays in Python where it's easier to update without redeploying PHP to every client site. | **Generic tool**, per today's proposed architecture. |
| 6 | Is the live app (`http://54.80.253.215`) meant to stay open to the whole internet, or do we need a real domain + HTTPS + login before wider use? | Confirmed today: currently reachable by anyone, not just your team, with no login. Fine for solo testing, not for real client-facing use. | **Buy a domain + add HTTPS + basic auth/login before onboarding any real client** — flagged, not yet acted on. |
| 7 | Priority order for the remaining infra gaps: Postgres migration, automated SQLite backups, instance/container crash monitoring? | All three are real risk (data loss, undetected downtime) but none are urgent today given current low usage. | **Backups first** (cheapest, protects existing real client data already in the DB), then monitoring, then Postgres only once usage actually grows past what SQLite comfortably handles. |
| 8 | Which caching plugins should the fleet-wide plugin explicitly support for cache-purge-after-deploy (LiteSpeed, WP Rocket, W3 Total Cache, WP Super Cache, SiteGround Optimizer, Cloudflare)? | No universal WordPress cache-purge API exists — confirmed today. Each needs its own detection + purge call, and a site can run more than one at once. | **Support LiteSpeed + WP Rocket + Cloudflare first** (most common among real client hosting setups) — extend the list per-client as needed rather than trying to cover every plugin on day one. |

---

### Immediate Next Steps (pick up from here)
1. Get client answers to the 8 questions above
2. Once confirmed: scope + build the new VtechSEO-Plugin v1 (auth + generic post-meta tool + core checksum scan)
3. Build the cache-purge-after-deploy fix (blocked on question 8's answer)
4. Backlink Gap module (scoped, `domain_intersection` verified live, not yet built)

### Known Loose Ends (carried over, unchanged today)
- `Suggestion.source` still hardcoded `"claude"` regardless of actual provider
- Postgres migration, monitoring/auto-recovery, db backup, domain+HTTPS — all still open

### Status
- Gemini provider: ✅ COMPLETE (Claude default, switchable)
- Dual-provider Backlinks: ✅ COMPLETE
- Competitor Analysis Phase 1-3: ✅ COMPLETE, stopped for review per instruction
- Backlink Gap: 🔜 SCOPED, NOT BUILT
- WordPress plugin RankMath support: 🔜 SCOPED, NOT BUILT (blocked on question 5)
- New VtechSEO-Plugin: 🔜 SCOPED, NOT BUILT (blocked on questions 2, 3)
