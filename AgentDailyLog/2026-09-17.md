# AgentLog — 2026-09-17

## Session: Image Alt Text fix-modal spike — thumbnails, cross-page grouping, media_id groundwork

---

### What We Did
- Spiked a shared `extract_image_alts()` helper (`app/html_extract.py`), used by both `app/crawler.py` and `app/dataforseo_onpage.py`'s `fetch_image_alts()`, replacing two near-identical inline `<img>` scrapers. Resolves relative `src` to absolute via `urljoin`.
- Fix modal (on-page → Image Alt Text issue) now renders an actual image preview per missing-alt image (was a bare `src` string list before), stacked layout (full-width image on top, details below), `object-fit: contain` so nothing gets cropped, modal widened 620px → 760px.
- **Cross-page grouping**: a single image (logo, header/footer asset) reused across many pages was showing as N disconnected "missing alt" facts, one per page, with no signal it's actually one fix. `onpage_semrush.py`'s `onpage_view` now builds a project-wide `image_src -> {page urls}` index (in-memory, from data already loaded, no new query/API call) and attaches `also_on` per image. Verified live: `vseo.vtraffic.io`'s logo (`vtlogo991.png`) is missing alt on 91 pages — collapsed behind a `<details>` toggle by default (was an unreadable flat list of ~90 raw URLs before a user flagged it as noise).
- **Root-caused why the feature initially showed nothing**: not a code bug — all 297 existing `dataforseo`-sourced `Page` rows had `image_alts=[]`, stored before/without a working fetch, and never refreshed since `image_alts` is only (re)written at ingestion time (Site Audit / Instant Check), not computed live. Confirmed `fetch_image_alts()` itself works correctly against a live URL (37 images fetched successfully in a manual test).
- **Confirmed "fix once, applies everywhere" is not yet automated**: `image_alt` is not in `DEPLOYABLE_CATEGORIES` (`onpage_semrush.py`), so there's no Deploy button wiring for these AI suggestions yet — today it's still a manual WordPress edit.
- Found the real blocker for automating that: we only have the scraped image `src`, not WordPress's internal `media_id`, and the plugin's `update_media_meta` tool requires `media_id`.
- **Partial fix implemented today**: WordPress stamps editor-inserted images with a `wp-image-{ID}` CSS class — confirmed live on `vseo.vtraffic.io` (e.g. `class="attachment-full size-full wp-image-4842"`). `extract_image_alts()` now parses this and returns `media_id` for free, no extra request, for any image inserted via the block/classic editor. Theme-level images (logo, header/footer, Customizer-set) carry no such class — `media_id` stays `None` for those; fix modal now shows "WP media #4842" or "no WordPress media_id found (theme/logo image)" per image so it's visible which images already have a usable ID.
- Full inventory compiled of every API call in the on-page flow (DataForSEO's 4 real endpoints: `instant_pages`, `task_post`, `tasks_ready`, `pages`, plus `links`; the one plain-HTTP (non-billed) call `fetch_image_alts` makes directly to the target site; SEMrush's on-page path confirmed still dormant per existing `project_semrush_onpage_decision` memory).

### Files changed today
- `app/html_extract.py` — new shared helper + `_wp_media_id()` class-parsing addition
- `app/crawler.py`, `app/dataforseo_onpage.py` — now call the shared helper instead of inline scraping
- `app/routes/onpage_semrush.py` — `image_src_to_pages` cross-page index, `also_on` + `media_id` attached in `_missing_alt_images()`
- `app/templates/onpage_semrush.html` — fix-modal image rendering (thumbnail, stacked layout, collapsed also-on list, media_id display), modal width

All marked `SPIKE:` in comments — nothing committed yet, plain `git checkout` reverts cleanly if any of it doesn't hold up.

**Not yet verified**: `wp-image-N` class parsing tested against one live site (`vseo.vtraffic.io`) only — not confirmed across other client sites/themes, which may format editor image classes differently or strip them via a page builder.

---

## MCP Plugin Backlog (what needs building in `claude-wp-mcp` next)

These are scoped, not built. Needed to turn today's "also missing on 72 other pages" insight into an actual one-click fix instead of a manual WordPress edit.

| # | Tool to add | Why | Notes |
|---|---|---|---|
| 1 | `update_media_alt_by_url` (new MCP tool, `handler-media.php`) | Resolves a scraped image URL → `media_id` via WordPress core's `attachment_url_to_postid()`, then writes alt text in one call. This is the **only** path for theme-level images (logo, header/footer) since they have no `wp-image-N` class for us to read during scraping. | Register in `class-mcp.php` tool schema, `class-router.php` dispatch table + tool listing — same 3-file pattern as existing `update_media_meta`. Must fail loud (not silently no-op) when `attachment_url_to_postid()` returns 0 — e.g. hotlinked/CDN images, or post-migration URL mismatches, genuinely have no resolvable attachment. |
| 2 | App-side wrapper `update_media_alt_by_url()` in `app/wordpress.py` | Mirrors existing `update_media_alt_text(media_id, alt)` — needed before the app can call tool #1. | |
| 3 | Add `image_alt` to `DEPLOYABLE_CATEGORIES` (`onpage_semrush.py`) | Currently `image_alt` suggestions have no Deploy button at all — only Accept/Reject/Use-in-editor. | Deploy path needs to branch: if `media_id` already known (from `wp-image-N` class) call plugin's existing `update_media_meta`; else call new tool #1 with the URL. |
| 4 | `fmDeploy()` (onpage_semrush.html) needs to pass either `media_id` or `src` depending on which is available per image | Front-end plumbing to match #3. | |

**Decision needed before building**: for images resolved via `also_on` cross-page grouping, should Deploy fix just the current page's image, or explicitly re-verify/re-flag all N pages listed in `also_on` after a WordPress-side fix (since WP writes are attachment-level and should auto-propagate, but this hasn't been confirmed against a real deploy yet)?

### Next
- Re-run a Site Audit on a project with real `image_alt` issues to confirm `media_id` populates correctly end-to-end from a fresh ingestion (today's manual test only proved `fetch_image_alts()` works standalone, not the full stored-in-DB path).
- Build MCP backlog items 1–4 above, one at a time per the task-list workflow (build, verify, push, then next).
- Revisit whether `fetch_image_alts`'s sequential per-page HTTP calls during `_store_page_result`'s ingestion loop should be made concurrent — flagged as a real (if secondary) slowness contributor to Site Audit refresh time, not yet fixed.
